{
  "name": "lru-cache",
  "version": "1.0.0",
  "description": "LRU Cache with O(1) get/put (hash map + doubly linked list) and optional TTL, implemented from scratch.",
  "main": "src/lru_cache.js",
  "type": "commonjs",
  "scripts": {
    "test": "node --test tests/test_lru_cache.js",
    "demo": "node demo.js"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "MIT"
}
